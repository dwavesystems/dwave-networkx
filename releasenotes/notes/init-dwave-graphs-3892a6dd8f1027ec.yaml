---
prelude: >
    Create ``dwave-graphs`` package based off ``dwave-networkx``.
    ``dwave_networkx`` namespace is renamed to ``dwave.graphs``.
upgrade:
  - |
    Drop the default sampler interface previously available in ``dwave-networkx``.
    Some algorithms now explicitly require a sampler.
